<?xml version="1.0" encoding="utf-8"?>
<!--
Usar ConstraintLayout para posicionar facilmente o menu em baixo
e o 'container' a preencher o resto do espaço
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.MainActivity"> <!-- NOTA: O teu context .ui.main.MainActivity está correto -->

    <!--
    O 'ecrã' principal.
    Este é o contentor que vai ser substituído pelos teus Fragments
    (DashboardFragment, BeneficiariosFragment, etc.)
    -->

    <!--
    Ligação ao gráfico de navegação.
    O erro que tinhas antes era porque os comentários
    estavam DENTRO da tag. Movi-os para aqui para cima.
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_nav_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <!-- A barra de navegação no fundo -->
    <!-- Ligação ao ficheiro de menu que criámos (agora no sítio certo) -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?android:attr/windowBackground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        app:menu="@menu/main_menu" />

</androidx.constraintlayout.widget.ConstraintLayout>